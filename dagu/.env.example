# Copy to .env and adjust.
GENERIC_TIMEZONE=Europe/Madrid

# Same vars as launchd/n8n/temporal — used by the failure handler_on in the DAGs.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Required for machine-health-agent.yaml (type: agent + chat.completion via OpenRouter).
OPENROUTER_API_KEY=
